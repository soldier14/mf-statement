name: CI/CD Pipeline

on:
  workflow_run:
    workflows: ["Lint Check", "Test & Coverage"]
    types: [completed]

jobs:
  ci-status:
    name: CI Status Check
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'
    
    steps:
    - name: Check CI Status
      run: |
        echo "All CI checks passed successfully!"
        echo "Lint and Test workflows completed successfully"
